Basic OPAM Docker environment for Ubuntu, including a helper that installs
external dependencies via `apt-get` before attempting to compile the package.

Example usage:

```
$ ./build.sh ubuntu-trusty 4.01.0
```

